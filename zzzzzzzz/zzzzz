<!-- we use a middlewear named as cors to connet two ports for ex when we connect frontend to backend we connect via help of cors
morgan- if any of the rest a[i is created it will tell in consone which api is hit,what time etc]0
watch blue dok -->
